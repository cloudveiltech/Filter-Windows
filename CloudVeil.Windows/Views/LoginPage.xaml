<LightContentPage x:Class="CloudVeil.Windows.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CloudVeil.Windows.Views"
      mc:Ignorable="d" 
      Title="LoginPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <Border Background="Blue" Height="400" Margin="0,0,0,24" />
            <Label Foreground="Red" Content="{Binding ErrorMessage}" />

            <Label Content="Username" />
            <TextBox Margin="0,0,0,16" Text="{Binding Username}" />

            <Label Content="Password" />
            <PasswordBox Margin="0,0,0,16" x:Name="password" />

            <Button Content="Log in" Command="{Binding AuthenticateCommand}" />
        </StackPanel>
    </Grid>
</LightContentPage>
