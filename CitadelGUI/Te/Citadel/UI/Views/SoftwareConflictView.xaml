<local:BaseView x:Class="Te.Citadel.UI.Views.SoftwareConflictView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Te.Citadel.UI.Views"
                xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource DefaultTextBlockStyle}" />
        <Style TargetType="Label" BasedOn="{StaticResource BaseFontStyle}" />
    </UserControl.Resources>
    
    <StackPanel>
        <Label HorizontalAlignment="Center"
                   Style="{StaticResource HeaderStyle}"
                   Content="Software Conflict Detected" />

        <TextBlock
            Margin="0,20,0,0"
            Text="{Binding ConflictText}"
            />

        <TextBlock Margin="0,40,0,0">
            <Hyperlink NavigateUri="{Binding ConflictSupportArticle}" RequestNavigate="OnNavigate">Removal Instructions</Hyperlink>
        </TextBlock>
    </StackPanel>
</local:BaseView>
