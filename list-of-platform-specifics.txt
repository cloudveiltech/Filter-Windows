CloudVeilGUI.WPF    		PlatformTypes.Register<IFilterStarter>((arr) => new WindowsFilterStarter());
CloudVeilGUI.WPF    		PlatformTypes.Register<IGuiServices>((arr) => new WindowsGuiServices());
CloudVeilGUI.WPF    		PlatformTypes.Register<ITrayIconController>((arr) => new WindowsTrayIconController());
CitadelService				PlatformTypes.Register<IPlatformDns>((arr) => new WindowsDns());
CitadelService				PlatformTypes.Register<IWifiManager>((arr) => new WindowsWifiManager());
CitadelService				PlatformTypes.Register<IPlatformTrust>((arr) => new TrustManager());
CitadelService				PlatformTypes.Register<IPathProvider>((arr) => new WindowsPathProvider());
CitadelService				PlatformTypes.Register<ISystemServices>((arr) => new WindowsSystemServices(this));
Citadel.Core.Windows        PlatformTypes.Register<IPipeClient>((arr) => new WindowsPipeClient((string)arr[0], (arr.Length > 1) ? (bool)arr[1] : false));
Citadel.Core.Windows        PlatformTypes.Register<IPipeServer>((arr) => new WindowsPipeServer((string)arr[0]));
Citadel.Core.Windows        PlatformTypes.Register<IGUIChecks>((arr) => new WindowsGUIChecks());
Citadel.Core.Windows        PlatformTypes.Register<IAntitampering>((arr) => new WindowsAntitampering());
Citadel.Core.Windows        PlatformTypes.Register<INetworkInfo>((arr) => new NetworkListUtil());
Citadel.Core.Windows        PlatformTypes.Register<IAuthenticationStorage>((arr) => new RegistryAuthenticationStorage());
Citadel.Core.Windows        PlatformTypes.Register<IPathProvider>((arr) => new WindowsPathProvider());
